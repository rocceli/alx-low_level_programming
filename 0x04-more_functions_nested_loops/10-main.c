#include<stdio.h>
#include "main.h"

/**
 * main - Entry pont
 *
 * Return: always 0
 */
int main(void)
{
	print_triangle(2);
	print_triangle(10);
	print_triangke(1);
	print_trianglee(0);
	return (0);
}
